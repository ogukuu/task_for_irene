import 'package:flutter/material.dart';

class ActiveTaskView extends StatelessWidget {
  const ActiveTaskView({Key? key}) : super(key: key);

  static const routeName = '/active_task';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
